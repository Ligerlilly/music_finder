<h1>Bands</h1>

<% if Band.all.any? %>
  <ul>
    <% Band.all.each do |band| %>
      <li><a href="/bands/<%= band.id %>"><%= band.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>
    There are no bands add one
  </p>
<% end %>

<a href="/bands/new">Add Band</a>
